{
    "name": "Google Ad Conversions Plugin (with Zapier)",
    "url": "https://github.com/PostHog/google-ad-conversions-zapier-plugin",
    "description": "Sends Google Ads conversion events to a Zapier webhook",
    "main": "index.ts",
    "posthogVersion": ">= 1.25.0",
    "config": [
        {
            "markdown": "Zapier webhook URL: Full URL of the webhook, e.g. `https://hooks.zapier.com/hooks/catch/0123456/abcdef/`\n\nPostHog Host: PostHog Host URL from which to fetch action definitions (in testing, set to `http://localhost:8000`"
        },
        {
            "key": "zapier_webhook_url",
            "name": "Zapier webhook URL",
            "type": "string",
            "required": true
        },
        {
            "key": "host",
            "name": "PostHog Host",
            "type": "string",
            "required": false
        }
    ]
}