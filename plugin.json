{
    "name": "Google Ad Conversions Plugin (with Zapier)",
    "url": "https://github.com/PostHog/google-ad-conversions-zapier-plugin",
    "description": "Sends Google Ads conversion events to a Zapier webhook",
    "main": "index.ts",
    "posthogVersion": ">= 1.25.0",
    "config": [
        {
            "markdown": "Action map: Comma-separated list of action_id:Google conversion name.\n\nZapier webhook URL: Full URL of the webhook, e.g. `https://hooks.zapier.com/hooks/catch/0123456/abcdef/`"
        },
        {
            "key": "action_map",
            "name": "Action map: (action_id:Google conversion name)",
            "type": "string",
            "default": "11036:Sign up - cloud,11037:Sign up - self-hosted free,11038:Sign up - self-hosted paid",
            "required": true
        },
        {
            "key": "zapier_webhook_url",
            "name": "Zapier webhook URL",
            "type": "string",
            "required": true
        },
        {
            "key": "ph_host",
            "name": "PostHog Host Address",
            "type": "string",
            "default": "https://app.posthog.com/",
            "required": false
        },
        {
            "key": "ph_project_key",
            "name": "PostHog Project Key",
            "type": "string",
            "required": true
        },
        {
            "key": "ph_personal_key",
            "name": "PostHog Personal Key",
            "type": "string",
            "required": true
        }
    ]
}
